---
phase: 05-statistics-analytics
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/backend/statl/repositories/lists_repository.py
  - app/backend/statl/services/lists_service.py
  - app/backend/statl/repositories/admin_analytics_repository.py
  - app/backend/statl/services/admin_analytics_service.py
  - app/backend/statl/routes/admin.py
  - app/backend/statl/routes/questions.py
  - app/backend/statl/tests/test_lists.py
  - app/backend/statl/tests/test_admin_questions.py
  - app/frontend/src/components/lists/ListResultsPanel.tsx
  - app/frontend/app/(tabs)/stats.tsx
  - app/frontend/app/(admin)/EstatisticasAdmin.tsx
  - app/frontend/src/components/questions/SharedQuestionViewer.tsx
  - app/frontend/app/(professor)/ProfessorMenu.tsx
autonomous: true
requirements:
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05

must_haves:
  truths:
    - "Professor list analytics lead with at-risk students based on low accuracy, while non-submission/lateness remain secondary signals"
    - "Professor results still expose completion, question error rates, and full student detail, but the primary ordering answers 'quem está com dificuldade?' first"
    - "Admin stats screens stop showing a ranking-first student leaderboard and instead render operational KPIs: active users last 7 days, total answers, accuracy by topic, and activity counts by role"
    - "Shared question browsing no longer calls `/admin/questoes`; professor/shared flows use a neutral question-browse route while admin-only SQL access stays under `/admin/sql`"
    - "Admin retains access to the question viewer screen and SQL tab after the cleanup"
  artifacts:
    - path: "app/backend/statl/routes/admin.py"
      provides: "Operational admin analytics endpoint(s) rather than ranking-centric quiz-summary data"
      contains: "/stats/dashboard"
    - path: "app/backend/statl/repositories/lists_repository.py"
      provides: "Risk-oriented professor list analytics derived from canonical answer-level data"
      contains: "risk_students"
    - path: "app/frontend/src/components/lists/ListResultsPanel.tsx"
      provides: "Professor results UI that foregrounds at-risk students and question hot spots"
      contains: "Alunos em risco"
    - path: "app/frontend/app/(tabs)/stats.tsx"
      provides: "Admin operational KPI dashboard"
      contains: "/admin/stats/dashboard"
    - path: "app/backend/statl/routes/questions.py"
      provides: "Neutral shared question-browse endpoint for professor/admin question viewing"
      contains: "/browse"
    - path: "app/frontend/src/components/questions/SharedQuestionViewer.tsx"
      provides: "Shared viewer wired to the neutral browse route rather than admin namespace semantics"
      contains: "/questions/browse"
  key_links:
    - from: "ListResultsPanel.tsx"
      to: "GET /lists/<id>/results"
      via: "response includes `risk_students`, score distribution, and per-question hot-spot analytics"
    - from: "stats.tsx and EstatisticasAdmin.tsx"
      to: "GET /admin/stats/dashboard"
      via: "single operational dashboard payload for KPI cards, topic accuracy, and role activity"
    - from: "SharedQuestionViewer.tsx"
      to: "GET /questions/browse"
      via: "neutral shared question listing path used by professor and admin viewers"
---

<objective>
Upgrade professor and admin analytics to the Phase 5 product direction and finish the shared question-viewer cleanup so professor/shared flows no longer depend on admin namespace semantics.

Purpose: after the answer-history foundation exists, this plan turns professor list results into a risk-first analytics view, replaces the ranking-centric admin stats screens with an operational dashboard, and neutralizes the shared question-browser API path while keeping admin SQL tools isolated.

Output: richer professor list-results payload/UI, a new admin dashboard payload/UI, and a neutral shared question-browse route adopted by the shared viewer.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-statistics-analytics/05-CONTEXT.md
@.planning/phases/04-professor-lists/04-04-SUMMARY.md
@.planning/todos/pending/2026-04-10-move-shared-question-viewer-out-of-admin-namespace.md
</context>

<interfaces>
Required professor/admin analytics and shared-viewer contracts for this plan.

1. `GET /lists/<id>/results`

Response additions required on top of the current Phase 4 contract:
```json
{
  "summary": {
    "assigned_students": 12,
    "submitted_students": 9,
    "average_score_pct": 61.5,
    "highest_error_rate_pct": 75.0,
    "late_students": 2,
    "at_risk_students": 3
  },
  "risk_students": [
    {
      "student_id": 21,
      "student_name": "Ana",
      "student_status": "entregue",
      "score_pct": 42.0,
      "submitted_at": "2026-04-12T13:20:00",
      "risk_band": "critico"
    }
  ],
  "score_distribution": [
    { "bucket": "0-49", "count": 2 },
    { "bucket": "50-69", "count": 4 },
    { "bucket": "70-100", "count": 3 }
  ],
  "students": [...],
  "per_question": [...],
  "change_log": [...]
}
```

Risk rules:
- `risk_students` must be sorted worst-first
- primary risk signal is `score_pct`
- `student_status in ('nova', 'em_andamento')` may still appear, but low submitted accuracy must rank ahead of lateness-only states
- `risk_band` values must be exactly `critico`, `atencao`, or `ok`

2. `GET /admin/stats/dashboard`

Response 200:
```json
{
  "kpis": {
    "active_users_7d": 18,
    "total_answers": 420,
    "aluno_activity_7d": 390,
    "professor_activity_7d": 12
  },
  "accuracy_by_topic": [
    {
      "topic_id": 11,
      "topic_name": "Distribuição de frequências",
      "chapter_name": "Estatística Básica",
      "answered_count": 24,
      "accuracy_pct": 58.3
    }
  ],
  "role_activity": [
    { "role": "aluno", "count_7d": 390 },
    { "role": "professor", "count_7d": 12 }
  ]
}
```

Rules:
- all KPI totals must come from canonical answer/list activity sources added in Phase 5, not the old `/admin/stats/alunos` aggregate query
- the first admin render must show the KPI cards even when `accuracy_by_topic` is empty

3. `GET /questions/browse`

Response shape must match the current shared viewer contract returned by `/admin/questoes`:
- `questoes`
- `total`
- `page`
- `per_page`
- `pages`

Access rules:
- `admin` and `professor` may read this endpoint
- `can_manage` still reflects whether the authenticated user can edit/delete the row
- `/admin/sql` remains admin-only and is not moved
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend professor list results with risk-first analytics and score distribution</name>
  <files>
    app/backend/statl/repositories/lists_repository.py
    app/backend/statl/services/lists_service.py
    app/backend/statl/tests/test_lists.py
  </files>
  <read_first>
    - app/backend/statl/repositories/lists_repository.py — current summary/per-question/student result queries
    - app/backend/statl/services/lists_service.py — current `/lists/<id>/results` response shaping
    - app/frontend/src/components/lists/ListResultsPanel.tsx — current summary-first UI so the response shape matches the intended frontend consumer
    - app/backend/statl/repositories/answer_history_repository.py — canonical answer-level store introduced in Plan 05-01
  </read_first>
  <action>
Update the professor results pipeline so `GET /lists/<id>/results` returns the exact new fields from `<interfaces>`.

Repository work in `app/backend/statl/repositories/lists_repository.py`:
- keep the existing list ownership checks
- add canonical answer-level aggregations for the selected `list_id`
- compute `risk_students` sorted by:
  1. submitted students with the lowest `score_pct`
  2. then late submitted students with the same score
  3. then `em_andamento`
  4. then `nova`
- assign `risk_band` using these exact thresholds:
  - `critico` for submitted `score_pct < 50`
  - `atencao` for submitted `score_pct >= 50 and < 70`
  - `ok` for submitted `score_pct >= 70`
  - `atencao` for `em_andamento`
  - `critico` for `nova`
- compute `score_distribution` buckets exactly as `0-49`, `50-69`, and `70-100`
- add `late_students` and `at_risk_students` counts into `summary`

Service work in `app/backend/statl/services/lists_service.py`:
- serialize the new `risk_students` and `score_distribution` blocks
- keep existing `students`, `per_question`, and `change_log` blocks intact so the frontend can still show the full roster and audit trail

Test work in `app/backend/statl/tests/test_lists.py`:
- assert the new keys exist in the `/lists/<id>/results` response
- assert low-accuracy students appear before non-submitted students in `risk_students`
- assert `score_distribution` counts sum to `submitted_students`
  </action>
  <verify>
    <automated>
      cd /home/danielar/Desktop/staticlass && \
      grep -n "risk_students" app/backend/statl/repositories/lists_repository.py && \
      grep -n "score_distribution" app/backend/statl/services/lists_service.py && \
      grep -n "risk_band" app/backend/statl/tests/test_lists.py
    </automated>
  </verify>
  <acceptance_criteria>
    - app/backend/statl/repositories/lists_repository.py contains `risk_students`
    - app/backend/statl/repositories/lists_repository.py contains `score_distribution`
    - app/backend/statl/services/lists_service.py contains `late_students`
    - app/backend/statl/services/lists_service.py contains `at_risk_students`
    - app/backend/statl/tests/test_lists.py contains `risk_band`
    - `cd /home/danielar/Desktop/staticlass/app/backend && pytest statl/tests/test_lists.py -q` exits 0
  </acceptance_criteria>
  <done>Professor list analytics now foreground student risk and score distribution instead of only a flat summary/roster view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace ranking-centric admin stats with an operational dashboard and render the new professor/admin analytics UIs</name>
  <files>
    app/backend/statl/repositories/admin_analytics_repository.py
    app/backend/statl/services/admin_analytics_service.py
    app/backend/statl/routes/admin.py
    app/frontend/src/components/lists/ListResultsPanel.tsx
    app/frontend/app/(tabs)/stats.tsx
    app/frontend/app/(admin)/EstatisticasAdmin.tsx
  </files>
  <read_first>
    - app/backend/statl/routes/admin.py — current `/admin/stats/alunos` and `/admin/stats/aluno/<id>` ranking-centric queries
    - app/frontend/app/(tabs)/stats.tsx — current ranking/alunos tab UI
    - app/frontend/app/(admin)/EstatisticasAdmin.tsx — current duplicate admin analytics screen
    - app/frontend/src/components/lists/ListResultsPanel.tsx — current professor results rendering
    - app/frontend/src/components/admin/EstatisticasCompartilhadas.tsx — old ranking/student-card component currently driving admin stats
  </read_first>
  <action>
Create `app/backend/statl/repositories/admin_analytics_repository.py` and `app/backend/statl/services/admin_analytics_service.py` that produce the `GET /admin/stats/dashboard` payload from `<interfaces>`.

Backend rules:
- `active_users_7d` counts distinct active users with either answer-history activity in the last 7 days or professor list-management activity in the last 7 days
- `total_answers` counts all `answer_history` rows
- `aluno_activity_7d` counts answer-history rows by students in the last 7 days
- `professor_activity_7d` counts professor-authored list mutations/submissions/publications in the last 7 days using existing list/change-log tables
- `accuracy_by_topic` is sorted worst accuracy first so the hardest topics surface immediately

Update `app/backend/statl/routes/admin.py` to add `GET /admin/stats/dashboard` and keep `/admin/sql` unchanged.

Frontend work:
- rewrite `app/frontend/src/components/lists/ListResultsPanel.tsx` so the first drill-down block under the summary cards is an `Alunos em risco` section using `risk_students`
- keep `Questões`, full student roster, and `Histórico de alterações` below that risk section
- rewrite both `app/frontend/app/(tabs)/stats.tsx` and `app/frontend/app/(admin)/EstatisticasAdmin.tsx` to call `/admin/stats/dashboard`
- remove the ranking/alunos tabs from these two admin stats screens; the new screens should show KPI cards first, then topic-accuracy rows, then role-activity rows
- stop importing `AbaRanking` and `AbaAlunos` into those admin stats screens
  </action>
  <verify>
    <automated>
      cd /home/danielar/Desktop/staticlass && \
      grep -n "/stats/dashboard" app/backend/statl/routes/admin.py && \
      grep -n "Alunos em risco" app/frontend/src/components/lists/ListResultsPanel.tsx && \
      grep -n "/admin/stats/dashboard" "app/frontend/app/(tabs)/stats.tsx" && \
      grep -n "/admin/stats/dashboard" "app/frontend/app/(admin)/EstatisticasAdmin.tsx"
    </automated>
  </verify>
  <acceptance_criteria>
    - app/backend/statl/routes/admin.py contains `/stats/dashboard`
    - app/frontend/src/components/lists/ListResultsPanel.tsx contains `Alunos em risco`
    - app/frontend/src/components/lists/ListResultsPanel.tsx contains `Histórico de alterações`
    - app/frontend/app/(tabs)/stats.tsx contains `/admin/stats/dashboard`
    - app/frontend/app/(admin)/EstatisticasAdmin.tsx contains `/admin/stats/dashboard`
    - app/frontend/app/(tabs)/stats.tsx does not contain `AbaRanking`
    - app/frontend/app/(admin)/EstatisticasAdmin.tsx does not contain `AbaAlunos`
  </acceptance_criteria>
  <done>Professor analytics and admin analytics now match the Phase 5 product direction: risk-first for professors and KPI-first for admins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Neutralize shared question browsing while keeping admin SQL access intact</name>
  <files>
    app/backend/statl/routes/questions.py
    app/backend/statl/tests/test_admin_questions.py
    app/frontend/src/components/questions/SharedQuestionViewer.tsx
    app/frontend/app/(professor)/ProfessorMenu.tsx
  </files>
  <read_first>
    - app/backend/statl/routes/questions.py — existing professor/admin question endpoints and access middleware
    - app/backend/statl/routes/admin.py — current `/admin/questoes` implementation to neutralize without touching `/admin/sql`
    - app/frontend/src/components/questions/SharedQuestionViewer.tsx — current `/admin/questoes` caller
    - app/frontend/app/(professor)/ProfessorMenu.tsx — lingering professor route that still pushes `/(admin)/QuestaoViewer`
    - app/backend/statl/tests/test_admin_questions.py — existing browse/can_manage coverage
  </read_first>
  <action>
Move the shared question-listing contract out of admin namespace semantics.

Backend:
- add `GET /questions/browse` to `app/backend/statl/routes/questions.py`
- implement the exact same response shape the shared viewer already consumes today (`questoes`, `total`, `page`, `per_page`, `pages`, `can_manage`)
- allow both `admin` and `professor`
- keep `/admin/sql` admin-only and untouched
- keep `/admin/questoes` either removed from the shared viewer or as a compatibility alias, but no professor/shared frontend code may call it after this task

Frontend:
- update `app/frontend/src/components/questions/SharedQuestionViewer.tsx` to call `/questions/browse`
- preserve `returnTo` behavior and `can_manage` semantics
- update `app/frontend/app/(professor)/ProfessorMenu.tsx` so `Gerenciar Questões` routes to `/(app)/QuestionPicker` instead of `/(admin)/QuestaoViewer`

Tests:
- update `app/backend/statl/tests/test_admin_questions.py` to hit `/questions/browse`
- add an assertion that professor shared browsing still marks only professor-owned questions with `can_manage=true`
- add an assertion that `/admin/sql` still rejects professor access
  </action>
  <verify>
    <automated>
      cd /home/danielar/Desktop/staticlass && \
      grep -n "/browse" app/backend/statl/routes/questions.py && \
      grep -n "/questions/browse" app/frontend/src/components/questions/SharedQuestionViewer.tsx && \
      grep -n "/(app)/QuestionPicker" "app/frontend/app/(professor)/ProfessorMenu.tsx" && \
      grep -n "/questions/browse" app/backend/statl/tests/test_admin_questions.py
    </automated>
  </verify>
  <acceptance_criteria>
    - app/backend/statl/routes/questions.py contains `/browse`
    - app/frontend/src/components/questions/SharedQuestionViewer.tsx contains `/questions/browse`
    - app/frontend/src/components/questions/SharedQuestionViewer.tsx does not contain `/admin/questoes`
    - app/frontend/app/(professor)/ProfessorMenu.tsx contains `/(app)/QuestionPicker`
    - app/backend/statl/tests/test_admin_questions.py contains `/questions/browse`
    - `cd /home/danielar/Desktop/staticlass/app/backend && pytest statl/tests/test_admin_questions.py -q` exits 0
  </acceptance_criteria>
  <done>Shared question browsing is now neutral for professor/shared flows, while admin-only SQL capabilities remain isolated under the admin namespace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| professor analytics payload -> selected professor-owned list | Detailed student risk data must only be returned for the authenticated professor's own list |
| admin dashboard -> operational KPI queries | Admin-only global metrics must not leak to professor/student routes |
| shared viewer -> neutral browse endpoint | Neutralizing the route must not accidentally grant professor access to admin-only SQL capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Information Disclosure | `/lists/<id>/results` risk payload | mitigate | Keep professor ownership checks before any analytics query and serialize only the selected professor-owned list |
| T-05-09 | Information Disclosure | `/admin/stats/dashboard` | mitigate | Keep the endpoint under `@require_role('admin')` and avoid reusing it from shared/student routes |
| T-05-10 | Elevation of Privilege | Neutral question browse route | mitigate | Limit `/questions/browse` to `admin` and `professor` and keep `/admin/sql` on the admin blueprint with admin-only middleware |
| T-05-11 | Integrity | Risk ordering | mitigate | Compute risk order server-side from validated score/status data so the UI cannot reorder students into a misleading safer state |
</threat_model>

<verification>
After task completion:

1. `grep -n "/stats/dashboard" app/backend/statl/routes/admin.py`
2. `grep -n "Alunos em risco" app/frontend/src/components/lists/ListResultsPanel.tsx`
3. `grep -n "/questions/browse" app/frontend/src/components/questions/SharedQuestionViewer.tsx`
4. `cd /home/danielar/Desktop/staticlass/app/backend && pytest statl/tests/test_lists.py statl/tests/test_admin_questions.py -q`
5. `cd /home/danielar/Desktop/staticlass/app/frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- Professor results foreground low-accuracy students as the primary risk lens
- Admin stats screens present operational KPIs instead of a ranking-first student leaderboard
- Shared question browsing no longer depends on `/admin/questoes`
- Admin still has the question viewer plus SQL access after the cleanup
- Existing list/question tests cover the new response shapes and access boundaries
</success_criteria>

<output>
After completion, create `.planning/phases/05-statistics-analytics/05-03-SUMMARY.md` with:
- the final professor risk metrics and score-distribution shape
- the admin dashboard KPI payload and screen structure
- which frontend screens were moved off ranking-centric admin stats components
- the shared question-viewer route/API cleanup and how admin SQL access stayed isolated
</output>
