---
phase: 05-statistics-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/backend/statl/repositories/student_analytics_repository.py
  - app/backend/statl/services/student_analytics_service.py
  - app/backend/statl/routes/users.py
  - app/frontend/app/(app)/Statistics.tsx
  - app/frontend/app/(tabs)/profile.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-05

must_haves:
  truths:
    - "Student stats screen stops calling `/users/estatisticas` and `/users/historico` and instead loads a domain dashboard sourced from `answer_history`"
    - "Student dashboard section order is fixed: overview KPIs first, then chapter breakdown, then topic breakdown, then a 4-week accuracy trend"
    - "The dashboard shows at least total answers, overall accuracy, chapter accuracy rows, topic accuracy rows, and four weekly trend points with real data"
    - "Profile calendar no longer depends on legacy `quiz_resultados` history; it uses an answer-history-backed activity endpoint"
    - "No new chart dependency is introduced; trend and breakdown visuals are rendered with existing React Native/Tamagui primitives"
  artifacts:
    - path: "app/backend/statl/routes/users.py"
      provides: "Student analytics endpoints backed by canonical answer history"
      contains: "/analytics/dashboard"
    - path: "app/backend/statl/repositories/student_analytics_repository.py"
      provides: "SQL aggregations for overview, chapter/topic accuracy, 4-week trend, and 28-day activity"
      exports: ["get_student_dashboard", "get_student_activity_days"]
    - path: "app/frontend/app/(app)/Statistics.tsx"
      provides: "Domain-oriented student analytics dashboard"
      contains: "/users/analytics/dashboard"
    - path: "app/frontend/app/(tabs)/profile.tsx"
      provides: "Profile calendar hydrated from canonical activity days instead of legacy quiz history"
      contains: "/users/analytics/activity"
  key_links:
    - from: "Statistics.tsx useFocusEffect"
      to: "GET /users/analytics/dashboard"
      via: "single dashboard fetch for overview, chapter/topic breakdowns, and 4-week trend"
    - from: "profile.tsx useFocusEffect"
      to: "GET /users/analytics/activity"
      via: "28-day activity-day list converted into the existing calendar grid"
    - from: "users.py analytics routes"
      to: "student_analytics_repository.py"
      via: "answer-history aggregations grouped by chapter, topic, and week"
---

<objective>
Replace the legacy student statistics experience with a canonical answer-history dashboard and switch the profile activity calendar to the same source of truth.

Purpose: Phase 5 explicitly turns the student stats surface into a learning-domain dashboard instead of a summary/history page backed by `quiz_resultados`. This plan delivers the backend aggregations and the frontend cutover so students see overview KPIs, mastery breakdowns, and a 4-week trend using real per-answer history.

Output: new student analytics repository/service/routes, a rewritten `Statistics.tsx` dashboard, and a profile calendar that reads answer-history activity days rather than legacy quiz summaries.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-statistics-analytics/05-CONTEXT.md
@.planning/phases/03-gamification/03-04-SUMMARY.md
</context>

<interfaces>
Required student analytics API contracts for this plan.

1. `GET /users/analytics/dashboard`

Response 200:
```json
{
  "overview": {
    "total_answers": 42,
    "correct_answers": 31,
    "overall_accuracy_pct": 73.8
  },
  "chapters": [
    {
      "chapter_id": 1,
      "chapter_name": "Estatística Básica",
      "answered_count": 12,
      "correct_count": 9,
      "accuracy_pct": 75.0
    }
  ],
  "topics": [
    {
      "topic_id": 11,
      "topic_name": "Distribuição de frequências",
      "chapter_id": 1,
      "chapter_name": "Estatística Básica",
      "answered_count": 5,
      "correct_count": 4,
      "accuracy_pct": 80.0
    }
  ],
  "trend_4w": [
    {
      "week_start": "2026-03-16",
      "label": "16 mar",
      "answered_count": 8,
      "correct_count": 6,
      "accuracy_pct": 75.0
    }
  ]
}
```

Rules:
- `trend_4w` must always return exactly 4 week buckets ordered oldest -> newest
- each chapter/topic row must be derived from `answer_history` joined to `questions`, `chapters`, and `topics`
- if the student has no answers yet, all arrays are empty and overview numeric fields are zero

2. `GET /users/analytics/activity`

Response 200:
```json
{
  "days": ["2026-04-01", "2026-04-03", "2026-04-07"]
}
```

Rules:
- `days` contains unique `YYYY-MM-DD` strings from the last 28 days only
- profile calendar uses this endpoint instead of `/users/historico`

Frontend section contract for `Statistics.tsx`:
- Section 1: overview KPI cards
- Section 2: chapter breakdown rows
- Section 3: topic breakdown rows
- Section 4: 4-week trend
- No "Últimas tentativas" section remains on the screen
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical student analytics endpoints backed by `answer_history`</name>
  <files>
    app/backend/statl/repositories/student_analytics_repository.py
    app/backend/statl/services/student_analytics_service.py
    app/backend/statl/routes/users.py
  </files>
  <read_first>
    - app/backend/statl/routes/users.py — current `/users/estatisticas` and `/users/historico` routes
    - app/backend/statl/services/resultado_service.py — legacy response-shaping style to replace for analytics
    - app/backend/statl/repositories/resultado_repository.py — example aggregate-query style, but note it is legacy and quiz-summary-based
    - app/backend/statl/repositories/answer_history_repository.py — canonical source added in Plan 05-01
  </read_first>
  <action>
Create `app/backend/statl/repositories/student_analytics_repository.py` with two concrete query helpers:
- `get_student_dashboard(student_id: int) -> dict`
- `get_student_activity_days(student_id: int, days: int = 28) -> list[str]`

`get_student_dashboard(...)` must:
- aggregate from `answer_history ah`
- join `questions q`, `chapters c`, and `topics t`
- compute the exact response shape from `<interfaces>`
- return four weekly buckets covering the trailing 28 days even when some buckets have zero answers

Create `app/backend/statl/services/student_analytics_service.py` with:
- `student_dashboard_service(student_id)`
- `student_activity_service(student_id)`
that coerce DB numerics into JSON-safe `int`/`float` values.

Update `app/backend/statl/routes/users.py` to add:
- `GET /users/analytics/dashboard`
- `GET /users/analytics/activity`
Both routes must be `@jwt_required()` and must always use `get_jwt_identity()` for the student id instead of accepting any path/user-id parameter.

Do not delete the legacy `/users/estatisticas` or `/users/historico` routes in this plan, but the new student UI built in Tasks 2 and 3 must stop calling them.
  </action>
  <verify>
    <automated>
      cd /home/danielar/Desktop/staticlass && \
      grep -n "/analytics/dashboard" app/backend/statl/routes/users.py && \
      grep -n "/analytics/activity" app/backend/statl/routes/users.py && \
      grep -n "def get_student_dashboard" app/backend/statl/repositories/student_analytics_repository.py && \
      grep -n "def get_student_activity_days" app/backend/statl/repositories/student_analytics_repository.py
    </automated>
  </verify>
  <acceptance_criteria>
    - app/backend/statl/routes/users.py contains `/analytics/dashboard`
    - app/backend/statl/routes/users.py contains `/analytics/activity`
    - app/backend/statl/repositories/student_analytics_repository.py contains `trend_4w`
    - app/backend/statl/repositories/student_analytics_repository.py contains `accuracy_pct`
    - app/backend/statl/services/student_analytics_service.py contains `student_dashboard_service`
    - app/backend/statl/services/student_analytics_service.py contains `student_activity_service`
  </acceptance_criteria>
  <done>Canonical student analytics routes exist and no longer depend on aggregate-only `quiz_resultados` queries.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite `Statistics.tsx` as a domain dashboard with overview, chapter/topic mastery, and 4-week trend</name>
  <files>
    app/frontend/app/(app)/Statistics.tsx
  </files>
  <read_first>
    - app/frontend/app/(app)/Statistics.tsx — current summary/history screen to replace
    - app/frontend/app/(tabs)/profile.tsx — current calendar card visual language to stay visually compatible with the rest of the student experience
    - app/constants/style.tsx — existing palette/font system
    - .planning/phases/05-statistics-analytics/05-CONTEXT.md — section ordering and dashboard intent
  </read_first>
  <action>
Rewrite `app/frontend/app/(app)/Statistics.tsx` so it:
- fetches `GET /users/analytics/dashboard` on focus
- removes every call to `/users/estatisticas` and `/users/historico`
- renders sections in this exact order:
  1. overview KPI cards: `Total de respostas`, `Acertos`, `Precisão geral`
  2. chapter breakdown list with percentage label and progress bar per chapter
  3. topic breakdown list with percentage label and answered-count subtitle per topic
  4. 4-week trend using four custom bar/column views built from `trend_4w`

Rendering rules:
- use only existing React Native/Tamagui primitives; do not add a chart library
- keep the current Staticlass palette and mobile-first card layout
- if all counts are zero, show an explicit empty state that says analytics will appear after the student answers questions
- remove the old `Últimas tentativas` history section entirely
  </action>
  <verify>
    <automated>
      cd /home/danielar/Desktop/staticlass && \
      grep -n "/users/analytics/dashboard" "app/frontend/app/(app)/Statistics.tsx" && \
      grep -n "Total de respostas" "app/frontend/app/(app)/Statistics.tsx" && \
      grep -n "Precisão geral" "app/frontend/app/(app)/Statistics.tsx" && \
      grep -n "trend_4w" "app/frontend/app/(app)/Statistics.tsx"
    </automated>
  </verify>
  <acceptance_criteria>
    - app/frontend/app/(app)/Statistics.tsx contains `/users/analytics/dashboard`
    - app/frontend/app/(app)/Statistics.tsx contains `Total de respostas`
    - app/frontend/app/(app)/Statistics.tsx contains `Precisão geral`
    - app/frontend/app/(app)/Statistics.tsx does not contain `/users/estatisticas`
    - app/frontend/app/(app)/Statistics.tsx does not contain `/users/historico`
    - app/frontend/app/(app)/Statistics.tsx does not contain `Últimas tentativas`
  </acceptance_criteria>
  <done>The student stats screen now behaves as a learning-domain dashboard instead of a legacy summary/history page.</done>
</task>

<task type="auto">
  <name>Task 3: Switch the profile calendar to canonical activity days</name>
  <files>
    app/frontend/app/(tabs)/profile.tsx
  </files>
  <read_first>
    - app/frontend/app/(tabs)/profile.tsx — current `buildCalendar()` and `/users/historico` fetch usage
    - app/backend/statl/routes/users.py — new `/users/analytics/activity` contract from Task 1
  </read_first>
  <action>
Update `app/frontend/app/(tabs)/profile.tsx` so the aluno-only `useFocusEffect(...)` still fetches `/users/profile/<email>` and `/gamification/ranking?page=1`, but replaces `/users/historico` with `GET /users/analytics/activity`.

Calendar rules:
- build the existing 4x7 grid from `days: string[]`
- keep the current `Sequência` and `Ranking` copy already implemented
- if the activity fetch fails, preserve the previously rendered calendar instead of blanking the profile
- do not remove the current `buildCalendar()` helper; adapt it to consume a day-string list rather than raw history objects
  </action>
  <verify>
    <automated>
      cd /home/danielar/Desktop/staticlass && \
      grep -n "/users/analytics/activity" "app/frontend/app/(tabs)/profile.tsx" && \
      grep -n "buildCalendar" "app/frontend/app/(tabs)/profile.tsx"
    </automated>
  </verify>
  <acceptance_criteria>
    - app/frontend/app/(tabs)/profile.tsx contains `/users/analytics/activity`
    - app/frontend/app/(tabs)/profile.tsx does not contain `/users/historico`
    - app/frontend/app/(tabs)/profile.tsx contains `buildCalendar`
    - app/frontend/app/(tabs)/profile.tsx contains `Sequência:`
  </acceptance_criteria>
  <done>Profile activity visualization now uses the same canonical answer-history source as the student analytics dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated student -> analytics endpoints | Dashboard and activity routes must only expose the authenticated student's analytics rows |
| answer-history aggregates -> UI cards/charts | Empty or sparse data must render clearly so the screen does not imply false mastery values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | `/users/analytics/*` | mitigate | Derive the student id exclusively from `get_jwt_identity()` and never accept a user-id path/query parameter |
| T-05-06 | Tampering | Analytics response shaping | mitigate | Convert all numerics to plain JSON-safe numbers in the service layer so chart widths and labels are computed from validated values only |
| T-05-07 | Integrity | Empty analytics state | mitigate | Return explicit zeroed overview fields and empty arrays when no `answer_history` rows exist; frontend shows an explicit empty state rather than misleading bars |
</threat_model>

<verification>
After task completion:

1. `grep -n "/analytics/dashboard" app/backend/statl/routes/users.py`
2. `grep -n "/analytics/activity" app/backend/statl/routes/users.py`
3. `grep -n "/users/analytics/dashboard" "app/frontend/app/(app)/Statistics.tsx"`
4. `grep -n "/users/analytics/activity" "app/frontend/app/(tabs)/profile.tsx"`
5. `cd /home/danielar/Desktop/staticlass/app/frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- Student dashboard reads only from canonical answer-history-backed endpoints
- Screen order is overview -> chapters -> topics -> 4-week trend
- Dashboard shows real counts and percentages with a clear empty state for new students
- Profile calendar uses canonical activity-day data instead of legacy quiz history
- No new chart dependency is required to render the trend or breakdown visuals
</success_criteria>

<output>
After completion, create `.planning/phases/05-statistics-analytics/05-02-SUMMARY.md` with:
- the final student analytics endpoint payloads
- the UI sections shipped and their ordering
- the empty-state behavior for zero-history students
- the profile-calendar data-source cutover from legacy history to canonical activity days
</output>
