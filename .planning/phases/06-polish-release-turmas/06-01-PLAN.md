---
phase: 06-polish-release-turmas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/backend/statl/config.py
  - app/backend/statl/__init__.py
  - app/frontend/app/(public)/login.tsx
  - app/frontend/app/(tabs)/listas.tsx
  - app/frontend/app/(app)/QuizInProgressScreen.tsx
  - app/frontend/app/(app)/ResultScreen.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "All three user role flows (aluno, professor, admin) complete without 500 errors or crashed screens"
    - "Production Fly.io backend responds to API calls — no localhost references in config"
    - "Password reset link resolves correctly in production (not localhost:5000)"
    - "Image uploads path is accessible in production environment"
    - "Lint passes with zero errors on all modified frontend files"
  artifacts:
    - path: "app/backend/statl/config.py"
      provides: "APP_BASE_URL from env, MAIL_SERVER/PORT from env"
      contains: "APP_BASE_URL"
    - path: "app/backend/statl/__init__.py"
      provides: "No localhost hardcoding in email or URL construction"
  key_links:
    - from: "email_service.py"
      to: "APP_BASE_URL env var"
      via: "os.environ.get('APP_BASE_URL')"
      pattern: "APP_BASE_URL"
    - from: "Fly.io production"
      to: "PostgreSQL instance"
      via: "DATABASE_URL env var"
      pattern: "postgresql://"
---

<objective>
End-to-end validation of every user flow across all three roles, fixing blocking bugs found during walkthroughs, and confirming the production Fly.io environment is correctly configured.

Purpose: Establish a bug-free baseline before adding new features (calendar, turmas, email deep link UX). Any crash or 500 error uncovered here must be fixed before Wave 2 plans run.
Output: A working app against the Fly.io production PostgreSQL instance, zero blocking bugs in the happy-path flows, and a verified production configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-release-turmas/06-CONTEXT.md
@app/backend/statl/config.py
@app/backend/statl/__init__.py
@app/backend/statl/services/email_service.py
@app/frontend/app/(public)/login.tsx
@app/frontend/app/(tabs)/listas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix production configuration — APP_BASE_URL and email settings</name>
  <read_first>
    - app/backend/statl/config.py
    - app/backend/statl/__init__.py
    - app/backend/statl/services/email_service.py
    - app/backend/statl/routes/auth.py
  </read_first>
  <action>
    Audit every place the backend constructs URLs or reads env vars for external services. Fix the following confirmed issues:

    1. **`app/backend/statl/services/email_service.py`** — Both `send_verification_email` and `send_reset_email` already read `APP_BASE_URL` from env (`os.environ.get("APP_BASE_URL", "http://localhost:5000")`). The fallback `http://localhost:5000` is acceptable for local dev but verify this env var is documented.

    2. **`app/backend/statl/config.py`** — Read this file first. Ensure `MAIL_SERVER`, `MAIL_PORT`, `MAIL_USE_TLS`, `MAIL_USERNAME`, `MAIL_PASSWORD` are all loaded from environment variables (not hardcoded). If any are hardcoded, convert them to `os.environ.get()` with no insecure fallback (fallback should be `None` or empty string).

    3. **`app/backend/statl/__init__.py`** — Add to `_garantir_schema_incremental`: the `turma_id` column on `lists` will be needed by plan 06-04, but do NOT add it here — that belongs in 06-04. Verify there are no hardcoded localhost references in this file.

    4. **Remove debug `print` statements** — `email_service.py` line 62 has `print(current_app.config["MAIL_SERVER"])` and line 63 has `print(current_app.config["MAIL_PORT"])`. Remove both prints. They leak config to server logs.

    5. **Document required env vars** — In `app/backend/.env.example` (create if missing), list every required env var: `DATABASE_URL`, `SECRET_KEY`, `JWT_SECRET_KEY`, `APP_BASE_URL`, `MAIL_SERVER`, `MAIL_PORT`, `MAIL_USE_TLS`, `MAIL_USERNAME`, `MAIL_PASSWORD`, `MAIL_DEFAULT_SENDER`, `FLASK_SECRET_KEY`. Use placeholder values, not real credentials.
  </action>
  <verify>
    <automated>cd /home/danielar/Desktop/staticlass/app/backend && python -c "from statl import create_app; app = create_app(testing=True); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "print(" app/backend/statl/services/email_service.py` returns no matches (debug prints removed)
    - `grep -rn "localhost:5000" app/backend/statl/` returns only the fallback default in `email_service.py`, no hardcoded non-fallback occurrences
    - `cat app/backend/.env.example` contains all 10 env var names listed in the action
    - `python -c "from statl import create_app; app = create_app(testing=True); print('OK')"` prints `OK` without error
  </acceptance_criteria>
  <done>Production configuration is clean: no debug prints, all sensitive config from env vars, .env.example documents every required variable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual E2E walkthrough — all three roles against Fly.io</name>
  <read_first>
    - app/frontend/app/(public)/login.tsx
    - app/frontend/app/(tabs)/home.tsx
    - app/frontend/app/(tabs)/listas.tsx
    - app/frontend/app/(app)/QuizInProgressScreen.tsx
    - app/frontend/app/(app)/ResultScreen.tsx
  </read_first>
  <what-built>
    Task 1 cleaned backend config. Now manually walk through every user flow against the Fly.io production instance (or local backend if Fly.io is not yet accessible — note which was used).
  </what-built>
  <how-to-verify>
    Run the app with `cd app/frontend && npx expo start` and test against backend.

    **Aluno flow (D-14 — production env):**
    1. Register new account → expect "Cadastro realizado!" alert with email verification message
    2. Try to login before verifying email → expect clear error "Verifique seu email antes de fazer login"
    3. After verifying (check email or manually set `email_verified=TRUE` in DB for speed) → login succeeds → land on Home tab
    4. Go to Questions tab → select chapter + difficulty → start quiz → answer all questions → see Result screen
    5. Open Profile tab → verify XP updated, streak shows, calendar renders (even if static for now)
    6. Open Ranking tab → leaderboard loads with at least the test account

    **Professor flow:**
    1. Login as professor → land on Home tab
    2. Go to Listas tab → tap "Criar lista" → fill title + deadline → save draft → add questions → publish
    3. Go to "Gerenciar Listas" → see the published list → tap "Ver resultados"
    4. Check that assigned_students count is visible

    **Admin flow:**
    1. Login as admin → Home shows admin buttons
    2. Open "Gerenciar Questões" → question list loads
    3. Open "Gerenciar professores" and "Gerenciar alunos" → both load without error

    **Document any bugs found** as a list in your resume signal. Claude will fix them before this checkpoint is approved.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all flows pass. OR describe each bug found (screen, action, error message/behavior) and Claude will fix them, then re-run this checkpoint.
  </resume-signal>
  <action>
    Fix any blocking bugs surfaced during the walkthrough before marking this checkpoint approved. Common areas to check:
    - Import errors or missing screen files
    - API endpoints returning 500 (check Flask logs)
    - Navigation dead ends (back button does nothing, screen stuck)
    - Blank white screens with no content or loading indicator
    For each bug: read the relevant source file, apply minimal targeted fix, re-run `npm run lint` in app/frontend to confirm no new lint errors.
  </action>
  <acceptance_criteria>
    - All six role-specific flows listed above complete without crashes or 500 errors
    - No blank white screens observed on any screen
    - `cd app/frontend && npm run lint` exits with 0 errors (warnings acceptable if pre-existing)
  </acceptance_criteria>
  <done>Every user flow completes end-to-end without errors. Production environment validated. Lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → Flask config | Secret values read from OS environment; must never appear in source code |
| frontend → backend API | All requests require JWT Bearer token; backend validates on every route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | email_service.py debug prints | mitigate | Remove `print(MAIL_SERVER)` and `print(MAIL_PORT)` lines (Task 1) |
| T-06-01-02 | Information Disclosure | .env file in repo | accept | .env is in .gitignore; .env.example with placeholder values only |
| T-06-01-03 | Spoofing | JWT tokens | accept | 12-hour expiry already enforced; HS256 with env-provided secret |
</threat_model>

<verification>
- `python -c "from statl import create_app; app = create_app(testing=True)"` succeeds
- `npm run lint` in app/frontend exits 0 errors
- No `print(` in email_service.py
- .env.example exists with all 10 required env var names
</verification>

<success_criteria>
1. All three role flows complete without 500 errors or crashes
2. Production Fly.io backend connectivity confirmed (or clearly documented as "tested against local")
3. No debug prints remain in backend source
4. .env.example documents every required environment variable
5. Lint passes with zero errors on modified frontend files
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-release-turmas/06-01-SUMMARY.md` with:
- List of bugs found and fixed
- Production environment validation status (Fly.io connectivity confirmed / tested local)
- Any environment variables that still need to be set in Fly.io secrets
</output>
