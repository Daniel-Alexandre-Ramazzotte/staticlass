---
phase: 02-question-bank-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/frontend/src/components/CustomAccordion.tsx
autonomous: true
requirements: [QUEST-03]
must_haves:
  truths:
    - "Student sees all 7 source options as chips in the Personalizar accordion"
    - "ENEM chip sends value 'ENEM' (uppercase) matching the backend enum"
    - "Source chips display Portuguese labels in title case (Vestibular, Concurso, etc.)"
  artifacts:
    - path: "app/frontend/src/components/CustomAccordion.tsx"
      provides: "FONTES constant updated from 4 to 7 values with correct casing"
      contains: "{ label: 'ENEM', value: 'ENEM' }"
  key_links:
    - from: "FONTES constant"
      to: "GET /questions/filtered?source=ENEM"
      via: "sources state in questions.tsx → router params → QuizInProgressScreen"
      pattern: "value: 'ENEM'"
---

<objective>
Update the FONTES constant in CustomAccordion from 4 hardcoded values to the canonical
7-value list with correct Portuguese labels and uppercase ENEM value.

Purpose: Students on the free-practice screen can now filter quizzes by all 7 source
types. The ENEM casing fix (enem → ENEM) ensures the filter matches backend stored values
after the data migration in 02-01.

Output: One updated constant in CustomAccordion.tsx. No other changes to the component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-question-bank-expansion/02-CONTEXT.md
@.planning/phases/02-question-bank-expansion/02-UI-SPEC.md

<interfaces>
<!-- Key contracts the executor needs. Extracted directly from codebase. -->

From app/frontend/src/components/CustomAccordion.tsx (current state, lines 31-36):
```tsx
const FONTES = [
  { label: 'Apostila', value: 'apostila' },
  { label: 'Concurso', value: 'concurso' },
  { label: 'Vestibular', value: 'vestibular' },
  { label: 'ENEM', value: 'enem' },   // ← 'enem' lowercase — BUG to fix
];
```

From app/frontend/app/(tabs)/questions.tsx (consumer — no changes needed):
- sources state: `const [sources, setSources] = useState<string[]>([]);`
- Passes to accordion: `setSources={setSources}`
- Passes to quiz: `params.source = sources` (array of string values from FONTES)

The component already has:
- FilterButton helper (uses active/inactive chip pattern)
- ClearButton helper (✕ Limpar)
- `XStack flexWrap="wrap" gap="$2"` wrapping FONTES chips — 7 chips wrap naturally
- toggleS helper function (already exists in file)

No changes needed to: FilterButton, ClearButton, toggleS, Props type, render logic,
Accordion structure, or any consumer files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace FONTES constant with 7-value array (D-10, D-11, D-12)</name>
  <files>app/frontend/src/components/CustomAccordion.tsx</files>
  <read_first>
    - app/frontend/src/components/CustomAccordion.tsx (read the full file — especially lines 31-36 and the Fonte section in JSX around line 229-247)
  </read_first>
  <action>
Make ONE change to app/frontend/src/components/CustomAccordion.tsx:

Replace the current FONTES constant (lines 31-36) with:

```tsx
const FONTES = [
  { label: 'Vestibular', value: 'vestibular' },
  { label: 'ENEM',       value: 'ENEM' },
  { label: 'Concurso',   value: 'concurso' },
  { label: 'Olimpíada',  value: 'olimpíada' },
  { label: 'Lista',      value: 'lista' },
  { label: 'Apostila',   value: 'apostila' },
  { label: 'Outro',      value: 'outro' },
];
```

Ordering: Vestibular → ENEM → Concurso → Olimpíada → Lista → Apostila → Outro
(per D-11 + Claude's Discretion from 02-CONTEXT.md)

Critical: `value: 'ENEM'` must be uppercase to match the backend enum after migration.
The old `value: 'enem'` would cause the SQL WHERE clause to match nothing in MySQL
(Pitfall 2 from 02-RESEARCH.md).

Do NOT change anything else in the file — not FilterButton, not ClearButton, not
toggleS, not the Props type, not the render JSX. The XStack flexWrap="wrap" gap="$2"
already handles 7 chips wrapping across 2 rows.

'olimpíada' — note the accented í — copy exactly, do not use 'olimpiada'.
  </action>
  <verify>
    grep -A 9 "^const FONTES" app/frontend/src/components/CustomAccordion.tsx
  </verify>
  <acceptance_criteria>
    - `grep "value: 'ENEM'" app/frontend/src/components/CustomAccordion.tsx` returns a match (uppercase, D-11)
    - `grep "value: 'enem'" app/frontend/src/components/CustomAccordion.tsx` returns NO match (old lowercase removed, D-12)
    - `grep "value: 'olimpíada'" app/frontend/src/components/CustomAccordion.tsx` returns a match (accented)
    - `grep "value: 'lista'" app/frontend/src/components/CustomAccordion.tsx` returns a match
    - `grep "value: 'outro'" app/frontend/src/components/CustomAccordion.tsx` returns a match
    - `grep -c "label:" app/frontend/src/components/CustomAccordion.tsx` returns 7 more than before (7 FONTES entries)
    - The FONTES array has exactly 7 entries: verify with `grep -A 9 "^const FONTES" app/frontend/src/components/CustomAccordion.tsx | grep "label:" | wc -l` → 7
    - `grep "value: 'apostila'" app/frontend/src/components/CustomAccordion.tsx` returns a match (kept, D-02)
    - `grep "value: 'concurso'" app/frontend/src/components/CustomAccordion.tsx` returns a match (kept, D-02)
    - `grep "value: 'vestibular'" app/frontend/src/components/CustomAccordion.tsx` returns a match (kept)
    - cd app/frontend && npx tsc --noEmit 2>&1 | grep "CustomAccordion" || echo "No TS errors in CustomAccordion"
  </acceptance_criteria>
  <done>FONTES constant contains exactly 7 values in the specified order. ENEM value is uppercase. All Portuguese labels are in title case. The 4 old values (apostila, concurso, vestibular — kept; enem — replaced with ENEM) are correctly mapped. Three new values added: olimpíada, lista, outro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client chip selection → GET /questions/filtered?source= | source value from FONTES.value sent as query param |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | source query param | accept | Values are from a closed UI constant (FONTES array); user cannot type arbitrary values; backend already filters with parameterized SQL |
</threat_model>

<verification>
After task completes:
1. grep -A 9 "^const FONTES" app/frontend/src/components/CustomAccordion.tsx — shows 7 entries with correct values
2. grep "value: 'enem'" app/frontend/src/components/CustomAccordion.tsx — returns nothing (old value gone)
3. grep "value: 'ENEM'" app/frontend/src/components/CustomAccordion.tsx — returns a match
4. cd app/frontend && npx tsc --noEmit — zero new TypeScript errors
</verification>

<success_criteria>
- FONTES constant has exactly 7 entries in the order: Vestibular, ENEM, Concurso, Olimpíada, Lista, Apostila, Outro
- ENEM value is 'ENEM' (uppercase) matching the backend enum (D-11, fixes Pitfall 2)
- Old lowercase 'enem' value is gone (D-12)
- No other changes to the component — render logic, types, and helper functions unchanged
- TypeScript compiles without new errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-question-bank-expansion/02-03-SUMMARY.md`
</output>
